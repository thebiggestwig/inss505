course = "INSS405"

for letter in course:
    print(letter, end=" ")
    # this is an example of iterating over a string. the goal of a for loop is to use Python in cycling through the
    # declared string/array to list out its contents. this is incredibly useful when paired with if-else statements
    # because they can highlight specific elements of interest from long sequences of data
